# 🚀 Как задеплоить исправленного бота

## ✅ Что было исправлено

Я исправил **4 критические проблемы**, из-за которых ваш бот падал:

### 1. **Главная ошибка: asyncio.signal**
```python
# БЫЛО (неправильно):
loop.add_signal_handler(asyncio.signal.SIGINT, handle_shutdown)

# СТАЛО (правильно):
import signal
signal.signal(signal.SIGINT, handle_shutdown)
```

### 2. **Добавлены таймауты**
- Загрузка файла: 30 секунд
- Конвертация PDF: 30 секунд
- Теперь бот не зависает на долгих операциях

### 3. **Health Check для Fly.io**
- Добавлены endpoints `/` и `/health`
- Fly.io теперь знает что бот жив

### 4. **Улучшенная обработка ошибок**
- Отдельные обработчики для разных ошибок
- Автоматическая очистка временных файлов
- Подробное логирование

---

## 📋 Пошаговая инструкция

### Шаг 1: Откройте командную строку Windows

Нажмите `Win + R`, введите `cmd`, нажмите Enter

### Шаг 2: Перейдите в папку проекта

```bash
cd "C:\Users\HP\OneDrive\Рабочий стол\cv-poland-project"
```

### Шаг 3: Остановите старые машины (опционально)

```bash
flyctl machine list -a cv-poland-project
flyctl machine stop <ID_МАШИНЫ> -a cv-poland-project
```

Замените `<ID_МАШИНЫ>` на реальный ID из списка (например: `d8d624fed6e3d8`)

### Шаг 4: Задеплойте новую версию

```bash
flyctl deploy --remote-only -a cv-poland-project
```

Это займет 3-5 минут. Вы увидите процесс сборки Docker образа и деплоя.

### Шаг 5: Проверьте статус

```bash
flyctl status -a cv-poland-project
```

Должно быть:
```
Status
  Instance        	Process	Version	Region	State  	Health Checks
  d8d4523f297dd8	app    	12     	ams   	started	1 total, 1 passing
```

### Шаг 6: Посмотрите логи

```bash
flyctl logs -a cv-poland-project
```

Вы должны увидеть:
```
Бот запущен (LibreOffice)...
Webhook успешно установлен: https://cv-poland-project.fly.dev/webhook
Бот полностью запущен и ожидает запросов...
```

---

## ✅ Проверка работы

### 1. Проверьте Health Check
Откройте в браузере: https://cv-poland-project.fly.dev/health

Должно показать: `OK`

### 2. Проверьте Webhook
В браузере откройте:
```
https://api.telegram.org/bot8579290334:AAEkgqc24lCNWYPXfx6x-UxIoHcZOGrdLTo/getWebhookInfo
```

Должно быть:
```json
{
  "ok": true,
  "result": {
    "url": "https://cv-poland-project.fly.dev/webhook",
    "has_custom_certificate": false,
    "pending_update_count": 0,
    ...
  }
}
```

### 3. Протестируйте бота
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Отправьте `.docx` файл
5. Получите PDF за 5-10 секунд

---

## 🐛 Если что-то не работает

### Проблема: Бот не отвечает

**Решение 1:** Проверьте логи
```bash
flyctl logs -a cv-poland-project -f
```

**Решение 2:** Перезапустите машины
```bash
flyctl machine restart <ID> -a cv-poland-project
```

**Решение 3:** Пересоздайте деплой
```bash
flyctl machine destroy <ID> -a cv-poland-project
flyctl deploy --remote-only -a cv-poland-project
```

### Проблема: Долгая конвертация

Это нормально для больших файлов. Таймаут 30 секунд.

### Проблема: Ошибка "connection refused"

Подождите 2-3 минуты после деплоя, пока машины полностью запустятся.

---

## 📊 Мониторинг

### Смотреть логи в реальном времени
```bash
flyctl logs -a cv-poland-project -f
```

Нажмите `Ctrl+C` чтобы остановить.

### Проверить статус машин
```bash
flyctl machine list -a cv-poland-project
```

### Открыть дашборд Fly.io
```bash
flyctl dashboard -a cv-poland-project
```

Или откройте: https://fly.io/dashboard/cv-poland-project

---

## 📁 Что загружено на GitHub

✅ Все изменения уже на GitHub:
- Исправленный `bot.py`
- Оптимизированный `Dockerfile`
- Новый `README.md`
- Новый `DEPLOY.md` (детальная инструкция)
- Новый `.gitignore`

**Репозиторий:** https://github.com/autoalexai-sketch/cv-konwerter-bot

---

## 💡 Полезные команды

```bash
# Проверить версию
flyctl version

# Войти в контейнер
flyctl ssh console -a cv-poland-project

# Проверить LibreOffice внутри контейнера
flyctl ssh console -a cv-poland-project -C "soffice --version"

# Просмотреть переменные окружения
flyctl ssh console -a cv-poland-project -C "env"

# Посмотреть использование ресурсов
flyctl machine status <ID> -a cv-poland-project
```

---

## 📞 Что дальше?

1. **Задеплойте бота** (команды выше)
2. **Протестируйте** работу
3. **Проверьте логи** если есть проблемы
4. **Настройте оплату Premium** (замените ссылку в `bot.py` строка 187)

Если нужна помощь - пишите! 🚀
