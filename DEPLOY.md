# 🚀 Инструкция по деплою бота на Fly.io

## Что было исправлено

### Критические исправления:
1. ✅ **Ошибка asyncio.signal** - заменено на модуль `signal`
2. ✅ **Таймауты** - добавлены таймауты 30 сек для конвертации и загрузки
3. ✅ **Обработка ошибок** - улучшена с отдельными обработчиками для разных типов ошибок
4. ✅ **Health check** - добавлены endpoints `/` и `/health` для проверки работоспособности
5. ✅ **Webhook** - улучшена настройка с удалением старого и проверкой нового
6. ✅ **Graceful shutdown** - корректная остановка при перезапуске
7. ✅ **LibreOffice** - оптимизирован (только writer компонент)
8. ✅ **Очистка** - временные файлы удаляются даже при ошибках

## Деплой на Fly.io

### 1. Остановить текущие машины
```bash
flyctl apps list
flyctl status -a cv-poland-project
flyctl machine list -a cv-poland-project

# Остановить все машины
flyctl machine stop <machine-id> -a cv-poland-project
```

### 2. Задеплоить новую версию
```bash
cd "C:\Users\HP\OneDrive\Рабочий стол\cv-poland-project"

# Деплой с пересборкой Docker образа
flyctl deploy --remote-only -a cv-poland-project
```

### 3. Проверить статус
```bash
# Проверить статус приложения
flyctl status -a cv-poland-project

# Посмотреть логи
flyctl logs -a cv-poland-project

# Проверить webhook
flyctl ssh console -a cv-poland-project -C "curl http://localhost:8080/health"
```

### 4. Проверить webhook в Telegram
```bash
# Через браузер или curl
curl https://api.telegram.org/bot8579290334:AAEkgqc24lCNWYPXfx6x-UxIoHcZOGrdLTo/getWebhookInfo
```

## Проверка работоспособности

1. **Health Check**: https://cv-poland-project.fly.dev/health
2. **Telegram**: Отправить /start боту
3. **Конвертация**: Отправить .docx файл боту

## Ожидаемое поведение

- ✅ Бот отвечает на /start за 1-2 секунды
- ✅ Конвертация PDF за 5-10 секунд
- ✅ Health check возвращает "OK"
- ✅ В логах нет ошибок про asyncio.signal

## Если бот все еще не работает

### Проверить логи:
```bash
flyctl logs -a cv-poland-project -f
```

### Проверить машины:
```bash
flyctl machine list -a cv-poland-project
flyctl machine status <machine-id> -a cv-poland-project
```

### Перезапустить машины:
```bash
flyctl machine restart <machine-id> -a cv-poland-project
```

### Полная переустановка:
```bash
# Удалить все машины
flyctl machine destroy <machine-id> -a cv-poland-project

# Задеплоить заново
flyctl deploy --remote-only -a cv-poland-project
```

## Мониторинг

```bash
# Смотреть логи в реальном времени
flyctl logs -a cv-poland-project -f

# Проверить метрики
flyctl dashboard -a cv-poland-project
```

## Типичные проблемы

### 1. Бот не отвечает
- Проверить webhook: `/getWebhookInfo`
- Проверить логи: `flyctl logs`
- Проверить машины: `flyctl machine list`

### 2. Долгая конвертация
- Таймаут установлен на 30 секунд
- Для больших файлов может потребоваться больше памяти
- Проверить размер файла (макс 15 МБ)

### 3. Ошибки LibreOffice
- Проверить установку в контейнере
- Проверить права на папку `/tmp`
- Попробовать другой формат документа

## Полезные команды

```bash
# SSH в контейнер
flyctl ssh console -a cv-poland-project

# Проверить LibreOffice
flyctl ssh console -a cv-poland-project -C "soffice --version"

# Проверить Python версию
flyctl ssh console -a cv-poland-project -C "python --version"

# Проверить установленные пакеты
flyctl ssh console -a cv-poland-project -C "pip list"
```

## Контакты и поддержка

- GitHub: https://github.com/autoalexai-sketch/cv-konwerter-bot
- Fly.io Dashboard: https://fly.io/dashboard/cv-poland-project
