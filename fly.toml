# fly.toml app configuration file generated for cv-konwerter-bot
app = 'cv-konwerter-bot'
primary_region = 'ams'

[build]
  dockerfile = "Dockerfile"

[env]
  MAX_FILE_SIZE_MB = "15"
  RATE_LIMIT_PER_HOUR = "5"
  ENVIRONMENT = "production"

# НЕ НУЖЕН HTTP SERVICE ДЛЯ POLLING БОТА!
# Комментируем всю секцию http_service
#[http_service]
#  internal_port = 8080
#  force_https = true
#  auto_stop_machines = false
#  auto_start_machines = true
#  min_machines_running = 1
#  processes = ['app']

# ТАКЖЕ КОММЕНТИРУЕМ health checks (они для HTTP)
#[[http_service.checks]]
#  grace_period = "60s"
#  interval = "30s"
#  method = "GET"
#  timeout = "5s"
#  path = "/"

[vm]
  memory = '1gb'
  cpus = 1