# 🚨 КРИТИЧЕСКИЙ ФИКС - Машины достигли лимита перезапусков!

## ❌ Проблема

Машины достигли максимального количества перезапусков (10 раз).
Ошибка: "machine has reached its max restart count of 10"

## ✅ Что исправлено СЕЙЧАС

1. ✅ Убраны signal handlers (причина ошибок)
2. ✅ Упрощен shutdown logic
3. ✅ Обновлен Docker rebuild trigger
4. ✅ Улучшена обработка ошибок

## 🔥 ЧТО ДЕЛАТЬ ПРЯМО СЕЙЧАС (ОБЯЗАТЕЛЬНО!)

### ШАГ 1: Удалить ВСЕ старые машины

```bash
cd "C:\Users\HP\OneDrive\Рабочий стол\cv-poland-project"

# Получить список машин
flyctl machine list -a cv-poland-project

# Удалить каждую машину (замените ID на реальные)
flyctl machine destroy 8dd624fed6e3d8 -f -a cv-poland-project
flyctl machine destroy d8d4523f297dd8 -f -a cv-poland-project
flyctl machine destroy e0ab0d24e4abe508 -f -a cv-poland-project
```

Флаг `-f` означает force (принудительно, без подтверждения).

### ШАГ 2: Деплой с ПОЛНОЙ пересборкой

```bash
flyctl deploy --remote-only --no-cache -a cv-poland-project
```

Флаг `--no-cache` заставит Docker пересобрать ВСЁ с нуля!

⏳ Это займет 5-7 минут (полная пересборка).

### ШАГ 3: Проверить логи

```bash
flyctl logs -a cv-poland-project
```

Вы должны увидеть:
```
✅ Бот запущен (LibreOffice)...
✅ Webhook handler зарегистрирован на /webhook
✅ Сервер запущен на 0.0.0.0:8080
✅ Webhook успешно установлен: https://cv-poland-project.fly.dev/webhook
✅ Бот полностью запущен и ожидает запросов...
```

**НЕ должно быть:**
- ❌ "module 'asyncio' has no attribute 'signal'"
- ❌ "max restart count of 10"

### ШАГ 4: Проверить машины

```bash
flyctl machine list -a cv-poland-project
```

Должны быть НОВЫЕ машины со счетчиком рестартов = 0.

### ШАГ 5: Проверить health

```bash
curl https://cv-poland-project.fly.dev/health
```

Должно вернуть: `OK`

### ШАГ 6: Тестировать бота

1. Telegram → Найти бота
2. Отправить `/start`
3. Должен ответить СРАЗУ!

---

## 📊 Полная последовательность команд (копируйте)

```bash
# 1. Перейти в папку
cd "C:\Users\HP\OneDrive\Рабочий стол\cv-poland-project"

# 2. Посмотреть машины
flyctl machine list -a cv-poland-project

# 3. Удалить все машины (скопируйте ID из пункта 2)
flyctl machine destroy <ID1> -f -a cv-poland-project
flyctl machine destroy <ID2> -f -a cv-poland-project

# 4. Деплой с полной пересборкой (ВАЖНО!)
flyctl deploy --remote-only --no-cache -a cv-poland-project

# 5. Проверить логи
flyctl logs -a cv-poland-project

# 6. Проверить health
curl https://cv-poland-project.fly.dev/health

# 7. Тестировать бота
# Telegram → /start
```

---

## 🔍 Что изменилось в коде

### Было (ОШИБКА):
```python
signal.signal(signal.SIGINT, handle_shutdown)
signal.signal(signal.SIGTERM, handle_shutdown)
```

### Стало (ПРАВИЛЬНО):
```python
try:
    while True:
        await asyncio.sleep(3600)  # Простое ожидание
except (KeyboardInterrupt, SystemExit, asyncio.CancelledError):
    print("Получен сигнал остановки")
```

---

## ⚠️ ВАЖНО!

**ОБЯЗАТЕЛЬНО используйте `--no-cache` при деплое!**

Иначе Docker будет использовать старый кэш с ошибками.

---

## 🐛 Если всё ещё не работает

### Вариант А: Пересоздать приложение

```bash
# Удалить приложение
flyctl apps destroy cv-poland-project

# Создать заново
flyctl launch --now
```

### Вариант Б: Проверить внутри контейнера

```bash
flyctl ssh console -a cv-poland-project

# Внутри контейнера:
ps aux | grep python
curl http://localhost:8080/health
```

---

## 📞 Чек-лист

- [ ] Удалены ВСЕ старые машины
- [ ] Выполнен деплой с `--no-cache`
- [ ] В логах НЕТ ошибки про asyncio.signal
- [ ] Health check возвращает OK
- [ ] Бот отвечает на /start

---

## 💡 Почему это произошло?

Fly.io использовал закэшированные Docker слои со старым кодом.
Даже после git push новый код не применялся из-за кэша.

Флаг `--no-cache` решает эту проблему!

---

## 🚀 НАЧИНАЙТЕ ПРЯМО СЕЙЧАС!

Время исправления: **5-10 минут**

Удачи! 🎯
