# 🚀 Деплой веб-приложения на Render.com

## 📋 Что получим после деплоя:
- ✅ Публичный URL: `https://cv-konwerter-web.onrender.com`
- ✅ Бесплатный хостинг (без кредитной карты)
- ✅ Автоматический SSL (HTTPS)
- ✅ Работает 24/7
- ✅ Автоматический деплой при обновлении кода на GitHub

---

## 🎯 Шаг 1: Регистрация на Render.com

1. Откройте: **https://render.com**
2. Нажмите **"Get Started for Free"** (или **"Sign Up"**)
3. Выберите **"Sign up with GitHub"**
4. Авторизуйтесь через GitHub (autoalexai-sketch)
5. Разрешите доступ Render к вашим репозиториям

---

## 🎯 Шаг 2: Создание Web Service

1. После входа нажмите **"New +"** → **"Web Service"**
2. Найдите репозиторий: **cv-konwerter-bot**
3. Нажмите **"Connect"**

---

## 🎯 Шаг 3: Настройка сервиса

### Основные настройки:

| Поле | Значение |
|------|----------|
| **Name** | `cv-konwerter-web` |
| **Region** | `Frankfurt (EU Central)` |
| **Branch** | `main` |
| **Root Directory** | _(оставить пустым)_ |
| **Runtime** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `python web_app.py` |

### Environment Variables (Переменные окружения):

Нажмите **"Add Environment Variable"** и добавьте:

| Key | Value |
|-----|-------|
| `PORT` | `10000` |
| `PYTHON_VERSION` | `3.12.0` |

### Instance Type:

- Выберите: **"Free"** (бесплатный план)
- ⚠️ **Важно:** Бесплатный план "засыпает" после 15 минут без активности. Первый запрос может занять ~30 секунд.

---

## 🎯 Шаг 4: Запуск деплоя

1. Нажмите **"Create Web Service"**
2. Начнётся автоматический деплой
3. Ожидание: **5-10 минут**

### Что вы увидите в логах:

```
==> Cloning from https://github.com/autoalexai-sketch/cv-konwerter-bot...
==> Checking out commit 51e21b8...
==> Running build command 'pip install -r requirements.txt'...
==> Successfully installed aiogram-3.13.1 aiohttp-3.9.5 flask-3.0.0...
==> Deploying...
==> Starting service with 'python web_app.py'...
🚀 Starting CV Konwerter Web App...
==> Your service is live 🎉
    https://cv-konwerter-web.onrender.com
```

---

## 🎯 Шаг 5: Проверка работы

### 1. Health Check
Откройте: **https://cv-konwerter-web.onrender.com/health**

Должно показать: `OK`

### 2. Главная страница
Откройте: **https://cv-konwerter-web.onrender.com/**

Должны увидеть:
- ✅ Фиолетовый градиентный дизайн
- ✅ Переключатель языков 🇵🇱 🇬🇧 🇺🇦
- ✅ Drag & drop зону загрузки
- ✅ Секцию Premium (39 zł)
- ✅ Кнопку "Telegram бот" → https://t.me/cv_konwerter_bot

### 3. Тест конвертации
1. Переключите язык на 🇵🇱 Polski
2. Загрузите `.docx` файл
3. Нажмите "Konwertuj na PDF"
4. ⚠️ **Первый запрос может занять 30-60 секунд** (установка LibreOffice)
5. Скачайте готовый PDF

---

## ⚠️ Возможные проблемы и решения

### Проблема 1: Build failed — LibreOffice не установлен

**Решение:** Добавьте специальный build script

В настройках Web Service измените **Build Command** на:
```bash
bash build.sh
```

Или используйте этот расширенный build command:
```bash
apt-get update && apt-get install -y libreoffice-writer libreoffice-core fonts-liberation fonts-dejavu && pip install -r requirements.txt
```

⚠️ **Важно:** Для установки системных пакетов на Render.com Free Plan может потребоваться создать `Dockerfile`.

### Проблема 2: Сервис не запускается

**Проверьте логи:**
1. Откройте дашборд Render
2. Выберите ваш сервис
3. Вкладка **"Logs"**
4. Ищите ошибки

**Типичные ошибки:**
- `ModuleNotFoundError` → не установлены зависимости
- `Address already in use` → проблема с портом (проверьте переменную PORT)
- `soffice not found` → LibreOffice не установлен

### Проблема 3: Конвертация не работает

**Причина:** LibreOffice требует `apt-get` права, которых нет в Free Plan.

**Решение:** Используйте Docker деплой (см. ниже)

---

## 🐳 Альтернатива: Деплой через Docker

Если LibreOffice не устанавливается, используйте Docker:

### 1. Создайте `Dockerfile.web`:

```dockerfile
FROM python:3.12-slim

WORKDIR /app

# Установка LibreOffice
RUN apt-get update && \
    apt-get install -y libreoffice-writer libreoffice-core fonts-liberation fonts-dejavu && \
    rm -rf /var/lib/apt/lists/*

# Копирование файлов
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Настройка LibreOffice
RUN mkdir -p /tmp/.libreoffice && chmod 777 /tmp/.libreoffice
ENV HOME=/tmp

EXPOSE 10000

CMD ["python", "web_app.py"]
```

### 2. В настройках Render выберите:
- **Runtime:** Docker
- **Dockerfile Path:** `Dockerfile.web`

---

## 📊 Статистика Free Plan

| Параметр | Значение |
|----------|----------|
| **Цена** | $0 / месяц |
| **RAM** | 512 MB |
| **CPU** | Shared |
| **Bandwidth** | 100 GB / месяц |
| **Build минуты** | 500 / месяц |
| **Спящий режим** | После 15 мин без активности |
| **Холодный старт** | ~30 секунд |

---

## 🎯 Следующие шаги после деплоя

1. ✅ Проверить работу всех функций
2. ✅ Обновить ссылки в Telegram боте (если нужно)
3. ✅ Добавить домен (опционально)
4. ✅ Подключить Przelewy24 для платежей
5. ✅ Создать LinkedIn бота для привлечения клиентов

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Скриншот логов из Render
2. Скриншот настроек Web Service
3. Текст ошибки

---

**Удачи с деплоем!** 🚀
